@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Thoát", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Đăng nhập", "LogOn", "Account") ]
}